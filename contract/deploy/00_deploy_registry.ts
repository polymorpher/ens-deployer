import { HardhatRuntimeEnvironment } from 'hardhat/types'
import { ethers } from 'hardhat'

const f = async function (hre: HardhatRuntimeEnvironment) {
    // TODO
}
f.tags = ['Registry', 'Full']
export default f
